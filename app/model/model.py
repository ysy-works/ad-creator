from PIL import Image

def generate_ad_image(product_image: Image.Image, prompt: str) -> Image.Image:
    """
    모델 팀이 이 함수를 채워줄 예정.
    지금은 더미 결과(입력 이미지 그대로 반환)로 전체 흐름 테스트.
    """
    return product_image